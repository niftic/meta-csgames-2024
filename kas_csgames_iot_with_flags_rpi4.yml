header:
  version: 8
  includes:
    - kas_csgames_iot_base.yml

machine: raspberrypi4-64

local_conf_header:
  extra: |
    CSGAMES_FLAGS = "true"
    IMAGE_NAME = "${IMAGE_BASENAME}-with_flags_rpi4"
